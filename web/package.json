{
  "name": "brut-observatory",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Closed-circuit physiological observatory. Camera + WebGPU shaders as observation apparatus.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.21"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@webgpu/types": "^0.1.52",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
